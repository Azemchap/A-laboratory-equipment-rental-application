# Parse Server Configuration

# MongoDB Atlas Connection String (Parse Server uses DATABASE_URI)
# Replace <username>, <password>, <cluster-url> with your actual MongoDB Atlas credentials
DATABASE_URI=mongodb+srv://<username>:<password>@<cluster-url>/<database-name>?retryWrites=true&w=majority

# Parse Server App ID (use a unique identifier for your app)
APP_ID=myLabRentalApp

# Parse Server Master Key (generate a strong random key for security)
MASTER_KEY=myStrongMasterKey123!ChangeMe

# Parse Server URL (should match your server address)
SERVER_URL=http://localhost:5000/parse

# Server Port
PORT=5000

# Cloud Code Path
CLOUD=./cloud/main.js

# Allow client class creation (true/false)
CLIENT_CLASS_CREATION=true

# Session Length (in seconds, default: 31536000 = 1 year)
PARSE_SERVER_SESSION_LENGTH=31536000

# Node Environment
NODE_ENV=development
